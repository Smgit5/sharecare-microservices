spring:
  application:
    name: DONATION-SERVICE

# DB properties - start
  datasource:
    username: postgres
    password: smpostgres
    url: jdbc:postgresql://localhost:5432/sharecare_donation_db

  jpa:
    hibernate:
      ddl-auto: update
# DB properties - end

server:
  port: 8083

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka